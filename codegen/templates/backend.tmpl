package {{.PackageName}}

import (
	"context"

	"github.com/grafana/grafana-app-sdk/resource"
)

// {{.KindName}}Backend is implemented by developer-provided code which backs {{.KindName}}'s
// storage with something other than the SDK's generic, etcd/CRD-backed store. Implementations
// can be adapted into a rest.Storage via apiserver.NewCustomStorage, to serve {{.KindName}}
// through the aggregated API server without it being backed by a CRD.
type {{.KindName}}Backend interface {
	// Get retrieves a single {{.KindName}} by its identifier.
	Get(ctx context.Context, identifier resource.Identifier) (*{{.KindName}}, error)
	// List retrieves a list of {{.KindName}} in the given namespace matching the provided options.
	List(ctx context.Context, namespace string, opts resource.ListOptions) (*{{.KindName}}List, error)
	// Create creates a new {{.KindName}}, returning the created object as it is stored.
	Create(ctx context.Context, obj *{{.KindName}}) (*{{.KindName}}, error)
	// Update updates an existing {{.KindName}}, returning the updated object as it is stored.
	Update(ctx context.Context, obj *{{.KindName}}) (*{{.KindName}}, error)
	// Delete deletes an existing {{.KindName}} by its identifier.
	Delete(ctx context.Context, identifier resource.Identifier) error
	// Watch begins watching for changes to {{.KindName}} in the given namespace matching the provided options.
	Watch(ctx context.Context, namespace string, opts resource.WatchOptions) (resource.WatchResponse, error)
}
